#include <stdio.h>

void fivesum(int a[], int capacidade, int x) {
    int i, j, k, l, m;
    for (i=0; i<capacidade; i++) {
        if (a[i] < x) {
            for(j=0; j<capacidade; j++) {
                if(a[j]<x && a[j] != a[i]) {
                    for()
                }
            }
        }
    }
}

int main(){
    int a[11] = {2321, 321, 24, 291, 19, 301, 502, 10020, 30201, 2001, 10}
    x = 850;

    return 0;
}